using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mensajeNivelCompletado : MonoBehaviour
{
    Mesh textMesh;

    private void Awake(){
        textMesh = transform.GetComponent<Mesh>();
    }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
